name: "sds-local-volume"
stage: "General Availability"
descriptions:
  en: "Manages local block storage based on LVM."
  ru: "Управляет локальным блочным хранилищем на базе LVM."
requirements:
  deckhouse: ">= 1.71"
  modules:
    sds-node-configurator: ">= 0.5.14"
    snapshot-controller: ">= 0.0.0"
namespace: d8-sds-local-volume
